# RIGHT NOW photographic imagery pass

## Scope
- Preserve every timer, Fire investigation question, safety boundary, menu order, and navigation path.
- Replace the illustrated cooldown scene with a photographic sky/water image and restrained overlays.
- Give the 5-minute Good session a photographic sky/nature backdrop plus rotating `GRATEFUL FOR:` cues.
- Give the 3-minute Kids session a photographic mirror/reflection opening, followed later by a plant/growth-in-light image and the nudge “What do you want them to see?”
- Keep every module skippable and reusable; do not alter scoring or evidence.

## Technical details
- Generate cohesive, text-free photographic assets because the chat uploads are not mounted in the project sandbox.
- Store images through the project asset flow and import their pointer files in `RightNow.tsx`.
- Add only presentation-state logic based on existing elapsed time; preserve countdown durations and completion behavior.
- Add semantic overlay treatments in `src/styles.css`, including reduced-motion handling.
- Run the existing test suite and verify the RIGHT NOW flow at a mobile viewport with no console errors.
